# Backend

Backend-модуль монорепозитория Seyvix.

Инструкции по Docker, Compose, окружению, разработке и деплою находятся в корневом
[README](../README.md).

Локальные backend-проверки:

```bash
uv run pytest
uv run ruff check app tests
uv run mypy app
```
